import { TestBed } from '@angular/core/testing';

import { CriterescahierclausesfinancierestechniquestypeService } from './criterescahierclausesfinancierestechniquestype.service';

describe('CriterescahierclausesfinancierestechniquestypeService', () => {
  let service: CriterescahierclausesfinancierestechniquestypeService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(CriterescahierclausesfinancierestechniquestypeService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
